import Flag from './Flag';
import classes from './Header.module.css';

const Header = () => {
    return (
        <div>
            <header className={classes.header}>
                <h1 className={classes.headersec}>Select</h1>
            </header>

        </div>
    );
};

export default Header;